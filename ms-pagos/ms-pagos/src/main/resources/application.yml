spring:
  application:
    name: ms-pagos
  datasource:
    url: jdbc:mysql://localhost:3306/db_pagos?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8084
jwt:
  secret: ferreteria_secret_key_2024_must_be_at_least_256_bits_long_secure
  expiration: 86400000